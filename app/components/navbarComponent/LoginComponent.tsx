const LoginComponent = () => {
  return (
    // If logged in, then display User Name
    <div className="flex justify-end">
      <p className="items-center">Login</p>
    </div>
  );
};

export default LoginComponent;
